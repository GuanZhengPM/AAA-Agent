Search FULL raw transcripts of past AAA Agent sessions in this workspace — exact text, not summaries.

Call this tool when:
- The user refers to earlier work or history ("昨天/上次/之前说过", "yesterday", "last time", "we decided"), prior decisions, corrected values, past task outcomes, or files changed in an earlier session.
- A detail from THIS session is no longer in your visible context: long conversations are compacted, but every turn stays fully searchable here.
- Before redoing work that may already exist from a previous session.

Returns ranked excerpts with session ids and timestamps; read-only and cheap. Prefer one specific query over several vague ones. It never switches the active session.
